# Optional hint 3 — lab2 revision 2

The publisher applies a configured creation mask to each new file before atomic replacement. Correct that persistent setting so the intended reader group retains access, without granting access to other users.
